import { Container } from '@/components/ui';
import { Certifications, Profile } from '@/components/sections';

export default () => {
  return (
    <Container>
      <Profile />
      <Certifications />
    </Container>
  );
};
